/** The scroll-to-bottom arrow. Decorative: the button around it carries the accessible name. */
export function ArrowDownGlyph() {
  return (
    <svg className="glyph" viewBox="0 0 20 20" aria-hidden="true">
      <path
        d="M10 4 V14 M5 9.5 L10 14.5 L15 9.5"
        fill="none"
        stroke="currentColor"
        strokeWidth="1.6"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
