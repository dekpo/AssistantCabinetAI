"""Assistant Cabinet AI gateway.

Every client talks to this gateway, never to the inference runtime directly.
The gateway holds no document text: not in a file, a database, a log or a debug trace.
"""

__version__ = "0.1.0"
