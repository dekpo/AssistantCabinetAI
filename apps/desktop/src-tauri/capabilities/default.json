{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the practice window may do. Opening a folder dialog is the only file capability: reading, writing and renaming go through Rust commands, never through the webview. Clipboard access is an explicitly granted plugin, not the ambient browser API.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text"
  ]
}
