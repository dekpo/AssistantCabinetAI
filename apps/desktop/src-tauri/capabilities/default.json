{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the practice window may do. Opening a folder dialog is the only file capability: reading, writing and renaming go through Rust commands, never through the webview.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
